
@{
    ViewBag.Title = "c_unit_waiting";
    Layout = "~/Views/Shared/_AdmissionSideNav.cshtml";
}

<div class="col-md-9">
    <h2 style="color:black; font-family:'Book Antiqua';text-align:center;text-decoration:underline;">C-UNIT Waiting List</h2>
    <div>&nbsp;</div>
    <div class="Container">
        <input type="text" id="Search" class="form-control" />
    </div>
    <br />
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Serial No.</th>
                <th>Student Id</th>
                <th>Alloted Faculty</th>
                <th>Alloted Hall</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>

            @{
                foreach (var item in Model)
                {
                    <tr class="Search">
                        <td>@item.id</td>
                        <td>@item.studentId</td>
                        <td>@item.allotedFaculty</td>
                        <td>@item.allotedHall</td>
                        <td>
                            <a href="@Href("~/c_unit/c_check_post/"+item.studentId +"?"+"allotedFaculty="+item.allotedFaculty+"&"+"allotedHall="+ item.allotedHall)">Further Process</a>
                        </td>
                    </tr>

                }
            }

        </tbody>
    </table>
</div>

<div class="col-md-3">
    <h3 style="color:black; font-family:'Book Antiqua';text-align:center;text-decoration:underline;">Remaining Seat</h3>
    <div>&nbsp;</div>
    <h4 style="color:black; font-family:'Book Antiqua';">Faculty Of BBA:<span style="color:red;">&nbsp;<b>@ViewBag.cse_seat</b></span></h4>


</div>


<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script>
    $(document).ready(function () {
        function Contains(text_one, text_two) {
            if (text_one.indexOf(text_two) != -1)
                return true;
        }

        $("#Search").keyup(function () {
            var searchText = $("#Search").val();
            $(".Search").each(function () {
                if (!Contains($(this).text(), searchText)) {
                    $(this).hide();
                }

                else {
                    $(this).show();
                }

            });
        });
    });
</script>








